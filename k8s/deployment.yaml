apiVersion: apps/v1
kind: Deployment
metadata:
  name: eval-php-min
  labels:
    app.kubernetes.io/name: eval-php-min
    app.kubernetes.io/instance: eval-php-min
    app.kubernetes.io/component: web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eval-php-min
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: eval-php-min
        app.kubernetes.io/name: eval-php-min
        app.kubernetes.io/instance: eval-php-min
        app.kubernetes.io/component: web
    spec:
      containers:
      - name: eval-php-min
        image: eval-php-min:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 80
          initialDelaySeconds: 5
          periodSeconds: 5
          timeoutSeconds: 2
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 80
          failureThreshold: 30
          periodSeconds: 10
